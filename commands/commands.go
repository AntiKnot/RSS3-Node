package commands

import "io"

// Subscribe returns the rss3file of the user
func Subscribe(user string) (io.Reader, error) {
	return nil, nil
}

// Post updates the rss3file of the user in rss3 network
func Post(user string, rss3File io.Reader) error {
	return nil
}
